# meta-signal-aggregator skills

This is a meta-signal contract repo. Keep runtime collection, transcript reads,
repository inspection, daemon sockets, and policy implementation out of this
crate. Configuration is typed data; changes happen through the `Configure`
operation.
