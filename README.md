# meta-signal-aggregator

Meta Signal contract for configuring and observing the `aggregator` daemon's
collection sources. This contract owns configuration vocabulary only; runtime
collection and storage live in `aggregator`.
