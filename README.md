# meta-signal-aggregator

Meta Signal contract for configuring and observing the `aggregator` daemon's
collection sources, durable daemon-local fragile output index policy, and
optional read-only legacy recovery roots. This contract owns configuration vocabulary
only; runtime collection and storage live in `aggregator`.
